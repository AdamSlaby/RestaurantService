import {ChartType} from "./chart-type";

export interface ChartOptions {
  defaultChartType: ChartType;
  periodOption: boolean;
  restaurantIdOption: boolean;
  chartTypeOption: boolean;
  orderTypeOption: boolean;
}
